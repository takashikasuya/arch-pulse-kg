# IF-INFRA-001 — NATS JetStream Subject 規約 + CloudEvents Envelope
# 正本: building-os-ecosystem/interfaces/interfaces.yaml
# このファイルは CS 固有の実装メモを記載する

interface_id: IF-INFRA-001
name: "NATS JetStream Subject 規約 + CloudEvents Envelope"
direction: consumed
transport: nats
schema_ref: "interfaces/IF-INFRA-001/nats-subjects.asyncapi.yaml"

# CS 固有の実装メモ:
implementation_notes: ""
local_endpoint: ""
